Clip and ship Geoprocessing tool
================================

Given an image service URL and polygon geometry, downloads and clips part of the image service. Then organizes the data inside the mosaic dataset.